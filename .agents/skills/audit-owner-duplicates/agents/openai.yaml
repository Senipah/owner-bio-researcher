interface:
  display_name: "Audit Owner Duplicates"
  short_description: "Review possible duplicate live owner records"
  default_prompt: "Use $audit-owner-duplicates to run a read-only duplicate audit of live owner records and produce a manual-review report."
